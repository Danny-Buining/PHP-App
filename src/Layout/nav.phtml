<?php
//Navigatie
$render[":nav"] = "";
$nav =  [
    "<a href='/'>Home</a>",
    "<a href='/movies'>Music</a>",
    isset($_SESSION['user']) ? "<a href='/movie/add'>Add Music</a>": null,
    isset($_SESSION['user']) ? '<span>'.ucfirst($_SESSION['user']['email']).'</span>':"<a href='/login'>Login</a>",
    isset($_SESSION['user']) ? "<a href='/logout'>Uitloggen</a>": null,
];

foreach($nav as $link) {
    if($link) {
        $render[":nav"] .= $link . " | ".PHP_EOL;
    }
}